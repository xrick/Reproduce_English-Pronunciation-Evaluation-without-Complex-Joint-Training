================================================================================
✅ QUICK START - Remote Training (ALL FIXES APPLIED)
================================================================================

All fixes have been applied on Mac. Now transfer and run on remote.

────────────────────────────────────────────────────────────────────────────────
STEP 1: Transfer Files (On Mac)
────────────────────────────────────────────────────────────────────────────────

1. Edit TRANSFER_TO_REMOTE.sh:
   nano TRANSFER_TO_REMOTE.sh
   # Change: REMOTE_HOST="your-actual-host"

2. Run transfer:
   ./TRANSFER_TO_REMOTE.sh

────────────────────────────────────────────────────────────────────────────────
STEP 2: Start Training (On Remote)
────────────────────────────────────────────────────────────────────────────────

ssh user@remote

cd /datas/store162/xrick/prjs/Reproduce_English_Pronunciation_Evaluation

rm -rf src/output/paper_r64/

source venv/bin/activate

python src/train_single_config_remote.py --config paper_r64 --gpus 0

────────────────────────────────────────────────────────────────────────────────
EXPECTED OUTPUT (First 2 minutes)
────────────────────────────────────────────────────────────────────────────────

✅ YOU MUST SEE:

  🔧 Applying LoRA configuration to model...
  ✅ LoRA configuration applied - parameters are now trainable
  
  📊 【論文配置 r=64】參數統計:
    可訓練參數: 200,000,000 (3.5%)  ← MUST BE ~200M, NOT 0!
  
  {'loss': 6.98, 'epoch': 0.26}   ← Start ~7.0
  {'loss': 6.42, 'epoch': 0.51}   ← Decreasing
  {'loss': 5.89, 'epoch': 0.77}   ← Clear downward trend

❌ YOU MUST NOT SEE:

  UserWarning: None of the inputs have requires_grad=True
  可訓練參數: 0 (0.0%)
  loss: 10.6312, 10.6774, 10.68...  ← Stuck at 10.6

────────────────────────────────────────────────────────────────────────────────
WHAT WAS FIXED
────────────────────────────────────────────────────────────────────────────────

✅ LoRA Training: Added get_peft_model() to BOTH configs (r=320 & r=64)
✅ FP16 Support: Changed torch.bfloat16 → torch.float16 (TITAN RTX)
✅ AMP Disabled: Set fp16=False to prevent GradScaler errors

────────────────────────────────────────────────────────────────────────────────
FILES TRANSFERRED
────────────────────────────────────────────────────────────────────────────────

src/model_utility_configs.py      ← LoRA fixes + FP16
src/train_single_config_remote.py ← AMP disabled
src/compat_trainer.py             ← Transformers compatibility

────────────────────────────────────────────────────────────────────────────────
TROUBLESHOOTING
────────────────────────────────────────────────────────────────────────────────

If still seeing "requires_grad" warning:

1. Verify files transferred:
   ls -lh src/model_utility_configs.py src/train_single_config_remote.py
   
2. Check LoRA fix applied:
   grep -c "model = get_peft_model" src/model_utility_configs.py
   # Should output: 2

3. Force clean restart:
   rm -rf src/output/paper_r64/ src/__pycache__/
   python src/train_single_config_remote.py --config paper_r64 --gpus 0

────────────────────────────────────────────────────────────────────────────────

For detailed documentation, see: FIXES_APPLIED_SUMMARY.md

================================================================================
